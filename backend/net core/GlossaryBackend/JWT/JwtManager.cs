using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Principal;
using System.Threading.Tasks;
using System.Web;
using System.IdentityModel.Tokens.Jwt;
using Microsoft.IdentityModel.JsonWebTokens;
using System.Security.Claims;
using Microsoft.IdentityModel.Tokens;
using System.Text;

namespace GlossaryBackend.JWT
{
    static class JwtValidators
    {
        public static LifetimeValidator LifetimeValidator = 
            (
                DateTime? notBefore, 
                DateTime? expires, 
                SecurityToken token, 
                TokenValidationParameters parameters
            ) =>
        {
            return 
            notBefore != null && notBefore.Value.ToLocalTime() < DateTime.Now
            && expires != null && expires.Value.ToLocalTime() > DateTime.Now;
        };
    }

    public static class JwtManager
    {
        //Generated by:
        //System.Security.Cryptography.TripleDESCryptoServiceProvider bla = new System.Security.Cryptography.TripleDESCryptoServiceProvider();
        //string temp = Convert.ToBase64String(bla.Key);
        private const string SECRET = "";

        private static readonly SymmetricSecurityKey SecurityKey = new SymmetricSecurityKey(Convert.FromBase64String(SECRET));

        public static string GenerateToken(ClaimsPrincipal principal, TimeSpan expiresIn)
        {
            JwtSecurityTokenHandler tokenHandler = new JwtSecurityTokenHandler();

            SigningCredentials credentials = new SigningCredentials(SecurityKey, SecurityAlgorithms.HmacSha256Signature);

            SecurityTokenDescriptor tokenDescriptor = new SecurityTokenDescriptor()
            {
                IssuedAt = DateTime.Now,
                Expires = DateTime.Now.Add(expiresIn),
                NotBefore = DateTime.Now,
                Subject = new ClaimsIdentity(principal.Claims),
                SigningCredentials = credentials
            };

            JwtSecurityToken token = tokenHandler.CreateJwtSecurityToken(tokenDescriptor);

            return tokenHandler.WriteToken(token);
        }

        public static ClaimsPrincipal AuthenticateToken(string token)
        {
            JwtSecurityTokenHandler tokenHandler = new JwtSecurityTokenHandler();

            if (!tokenHandler.CanReadToken(token))
                return null;
            
            tokenHandler.ValidateToken(token, new TokenValidationParameters()
            {
                RequireExpirationTime = true,
                RequireSignedTokens = true,
                ValidateLifetime = true,
                ValidateAudience = false,
                ValidateIssuer = false,
                IssuerSigningKey = SecurityKey,
                LifetimeValidator = JwtValidators.LifetimeValidator,

            },out SecurityToken scToken);

            JwtSecurityToken jwt = scToken as JwtSecurityToken;

            if(jwt != null && jwt.Claims != null)
            {
                return new ClaimsPrincipal(new ClaimsIdentity(jwt.Claims));
            }
            else
            {
                return null;
            }
        }
    }
}